# Changelog

AI 서비스 엔지니어링 Track A 강의 사이트의 주요 변경 사항을 기록한다.

형식은 [Keep a Changelog](https://keepachangelog.com/en/1.0.0/)를 따르고,
[유의적 버전](https://semver.org/lang/ko/)을 준수한다. 릴리스는 저장소 루트의
git 태그(`vX.Y`)로, 사이트 버전은 `site/package.json`으로 관리한다.

## [1.9.0] - 2026-08-21

### 추가

- **도구 유무를 그림 두 장으로 대비 (5주차 3장)**: 같은 배치를 쓰되 ①은
  대화 화면과 모델 사이에서 고리가 닫히고 점선이 "닿을 길이 없다"로 끊기며,
  ②는 하네스와 도구를 지나 바깥 세상까지 뻗는다. 두 장을 넘기면 박스가
  움직이는 게 아니라 **길이 생긴다**. 데모 바로 앞에 배치했다
- **도구 유무 대비 예제 (5주차 6장 12절)**: `03_tools/11_calc_with_without.py`.
  "3+4+(5 곱하기 2)"에서는 도구가 있으나 없으나 둘 다 17로 맞는다. 그 착시가
  표적이다. 자릿수를 키운 두 번째 질문에서 도구 없는 쪽이 452,905,678을
  내놨고 정답은 452,913,128이다. 다시 돌리면 **틀린 값 자체가 달라진다**

### 변경

- **모델과 도구의 관계도를 다시 그렸다**: 바깥 세상이 서비스 박스 테두리에
  붙어 있어 "모델이 도구를 통해 바깥과 닿는다"가 보이지 않았고, 하네스가
  도구와 나란한 형제 상자로 그려져 있었다. 경로에 번호를 붙여 모델 → ① 실행
  요청 → 하네스 → ② 통과한 것만 → 도구 → ③ 바깥 → ④ 관찰 결과로 고쳤다
- **5주차 슬라이드에서 랩 세팅 브리핑 6장을 걷어냈다**: 랩 기동·check_env·
  잘 안 될 때·예제 실행 방법·비용. 덱은 세 장 만에 본론으로 들어간다.
  같은 내용은 교안 2장에 그대로 있다
- **예제 수**: 5주차 31 → 32종, 6주차 55 → 56종

### 수정

- **6주차 랩의 에이전트가 실행되지 않았다**: `agent/main.py`가 f-string 안에
  생 줄바꿈을 넣어 `SyntaxError`였다. 교안이 시키는
  `python -m agent.main --max-cost 0.10 …`이 그대로 죽었다. 유닛 테스트는
  33개 전부 통과하는데, v0.3에서 `test_main.py`가 사라지며 `main.py`가
  테스트 밖으로 나가 있었다
- **budget guard와 트레이스가 기본 경로에서 놀고 있었다**:
  `run_with_reflexion`이 `guard`·`logger`를 받지 않아 `--no-reflexion`을
  붙였을 때만 하네스가 붙었다. 누적 비용은 늘 $0.0000으로 찍히고 트레이스
  파일은 비어 있었다. **재시도가 곧 재호출이라 하네스가 가장 필요한 경로가
  무방비였다.** 고친 뒤 기본 실행에서 누적 비용 $0.0036과 트레이스 9줄을
  확인했다

## [1.8.0] - 2026-08-21

### 추가

- **5주차에 3장 "AI 모델과 AI 서비스"**: 예제를 열기 전에 좌표를 잡는 장이다.
  AI 모델은 텍스트를 넣으면 텍스트가 나오는 함수 하나이고, 우리가 쓰는
  제품(ChatGPT·Claude Code·Codex)은 그 위에 도구·루프·방어선을 둘러 붙인
  것이라는 구분에서 출발한다. 이어서 **도구의 정의**를 이름·설명·입력
  스키마 셋으로 세우고, 도구가 아닌 것(프롬프트의 부탁, 모델 안의 지식,
  미리 넣어 준 RAG 문서)까지 경계를 긋는다
- **만져 보는 그림 `<ModelVsService />`**: 질문 다섯 개를 같은 모델에 **도구
  없이 한 번, 도구를 쥐여주고 한 번** 실제로 물은 기록이다. 산수 질문에서
  모델이 망설임 없이 **112,267,021**을 내놨고 정답은 112,267,001이었다.
  거절 없이 틀리는 쪽이 위험하다는 것을 실측으로 보인다. 여행 추천
  질문에서는 도구를 쥐여줘도 부르지 않는 것까지 같은 화면에 담았다.
  추출 절차는 `docs/model-vs-service-data.md`

### 변경

- **5주차 장 번호가 한 칸씩 밀렸다**: LLM API의 본질 3장 → 4장, LiteLLM
  4장 → 5장, Tool calling 심화 5장 → 6장. 교안·슬라이드의 장·절 상호 참조를
  함께 옮기고, 6장 3절의 "우리가 매일 만나는 도구들"은 3장으로 옮겨 중복을
  없앴다. 슬라이드에도 3장 구간 12장을 새로 넣어 두 문서를 맞췄다

## [1.7.0] - 2026-08-21

### 변경

- **5·6주차에서 릴리즈 사다리를 걷어냈다**: 태그를 오르내리며 시연하는 대신
  **완성본 하나(`main`)로 진행**한다. 교안의 사다리 절·릴리즈 표·
  `git checkout` 블록을 없애고, "vX.Y 코드 포인트"는 "코드 포인트"로,
  "v0.2로 오릅니다"는 그 지점에서 실제로 일어나는 일로 바꿨다. 두 실습 랩의
  README·CLAUDE.md와 코드 주석의 버전 표기도 함께 정리했다. 라이브로 코드를
  만들어 가는 1\~4주차 시연 저장소는 그대로 릴리즈 사다리를 쓴다
- **5·6주차 슬라이드에 목차 계층을 세웠다**: 장 구분자만 `h2`로 두고 장 안의
  슬라이드를 한 단계씩 내렸다. Part는 `h3`, 개별 슬라이드는 `h4`다. 테마가
  슬라이드의 첫 헤딩을 레벨과 무관하게 같은 크기로 렌더하므로 화면은
  그대로이고 목차만 **장 → Part → 슬라이드** 3단으로 접힌다

## [1.6.0] - 2026-08-21

### 추가

- **5주차 교안·슬라이드 (session-05)**: "API 해부와 tool calling". 1\~4주차에서
  `instructor`와 LiteLLM 뒤에 가려 있던 API 호출의 가림막을 걷는다. 최소 호출의
  요청·응답 JSON 원문에서 출발해 역할·토큰·컨텍스트 한도·무상태·비용을 잡고,
  통합 레이어 위에서 교체·폴백·캐싱·라우팅을 쓰고, 도구 호출 왕복을 JSON
  원문으로 해부한다. **예제 31종 전부의 실제 실행 결과**를 싣고, 세션 끝에서
  여행 플래너 에이전트가 v0.2로 오른다. 슬라이드 147장
- **6주차 교안·슬라이드 (session-06)**: "에이전트 루프와 하네스". 도구를 든
  에이전트에 루프를 달아 처음으로 동작시키고(v0.3), ReAct의 비용 구조를
  공격하는 루프들을 견주고(v0.4), 인젝션 사고를 재현해 데이터 경계·권한
  게이트·budget guard로 막아 v1.0에서 완성한다. 마지막으로 도구를 표준
  프로토콜로 주고받는 MCP까지 간다(v1.2) **예제 24종 전부의 실제 실행 결과**를
  싣는다. 슬라이드 205장
- **시연 저장소 두 개**: [week05-agent-lab](https://github.com/2026-ai-service-engineering-a/week05-agent-lab)(예제
  31종 + 에이전트 뼈대, v0.1 → v0.2)과
  [week06-agent-lab](https://github.com/2026-ai-service-engineering-a/week06-agent-lab)(예제
  55종, v0.2 → v1.2)이다. 6주차 랩의 **첫 태그가 v0.1이 아니라 v0.2**다. 5주차가
  끝난 지점이 6주차의 출발점이라 번호를 이어 받았고, 그 시점의
  `agent/`·`tests/`는 양쪽이 같다. 각 태그에서 유닛 테스트가 전부 통과하는
  것을 확인했다 (5/16/17/21/33개)
- **만져 보는 그림 두 개**: `<ContextGrowth />`는 messages 배열이 스텝마다
  부푸는 것을 스텝별로 넘겨 보게 하고(9번 호출·11,433토큰의 실측, compact
  전후 포함), `<HarnessGates />`는 경로 감금·출력 필터·도구 권한·budget guard
  네 게이트를 직접 두드려 보게 한다. 판정 모듈은 파이썬 원본과 29건을 대조해
  전부 일치하는 것을 확인했다
- **용어집 9종**: tool-calling, react, plan-execute, reflexion, rewoo, harness,
  budget-guard, mcp, release-ladder

### 변경

- **5·6주차 주제 교체**: "임베딩·벡터DB / RAG 기본 구현"에서 "API 해부와 tool
  calling / 에이전트 루프와 하네스"로. 4주차가 이미 정제부터 임베딩·벡터
  인덱스·질의응답까지 RAG 전 과정을 다뤘으므로, 두 주를 도구와 에이전트에 쓴다
- **커리큘럼 표 일괄 갱신**: README·소개 페이지의 주차표, 1주차 교안과
  슬라이드의 14주 흐름도·5레이어 채우기 표·엔지니어링 사다리 매핑, RAG 개념
  문서의 주차 연결표

## [1.5.0] - 2026-08-21

### 추가

- **GitHub Pages 배포**: `main`에 push되면 `site/`를 정적 빌드해 올리는
  워크플로(`.github/workflows/deploy.yml`)를 붙였다. 공개 주소는
  `https://2026-ai-service-engineering-a.github.io/ai_service_engineering-track_a/`
  이고, git flow대로 release·hotfix가 `main`에 머지될 때 배포된다
- **배포 문서(`docs/deployment.md`)**: 저장소 최초 설정(Settings > Pages의
  Source를 GitHub Actions로), base 경로 대응표, 워크플로가 하는 일, 커스텀
  도메인 전환 방법, 배포가 안 될 때의 증상별 확인 목록

### 변경

- **프로젝트 사이트 base 경로**: `site`를 `https://2026-ai-service-engineering-a.github.io`,
  `base`를 `/ai_service_engineering-track_a`로 맞췄다. 로컬 개발 서버와
  `docker compose up`도 같은 경로로 접속한다
- **본문 내부 절대 링크에 base 자동 접두**: `[텍스트](/article/…)`는 그대로
  두면 프로젝트 사이트에서 404가 난다. remark 플러그인이 빌드 시 base를 붙이므로
  콘텐츠는 base를 몰라도 된다
- **저장소 공개 전환 반영**: 헤더의 GitHub 링크를 노출하고(`repoNav`),
  private(수강생 전용) 콘텐츠를 쓰지 않는다는 원칙을 문서에 명시했다

## [1.4.0] - 2026-08-13

### 추가

- **4주차 슬라이드 덱 (session-04)**: 교안 전 범위를 담은 128장짜리 라이브
  세션용 덱. 더러움 3종과 처방, 비용 사다리, 세 회전, 임베딩 조립안 실험,
  RAG 질의응답, 3주차 되돌아가기까지
- **RAG 검색 회전 (3회전 `feature/rag-search`)**: 정제 데이터셋 위에 구글
  임베딩(`gemini-embedding-001`)으로 벡터 인덱스를 세워 의미 검색
  `GET /foods/semantic`과 질의응답 `POST /ask`를 연다. 30만 건은 컨텍스트에
  들어가지 않으니 검색이 추린 다섯 건만 LLM에 준다는 RAG의 비용 논리를
  규모로 보인다
- **임베딩 조립안 A/B 프로브 (6-1절)**: 무엇을 벡터로 만들지 추측하지 않고
  잰다. 샘플 1,000건에 조립안 4종을 2센트로 재 둘을 버리고, 남은 둘만 전량
  306,307건으로 확인했다. **1,000건에서 이겼던 조립안이 전량에서 뒤집힌
  기록**을 그대로 싣는다
- **벡터 DB 대조군**: memmap과 numpy로 짠 전수 비교(0.6초·recall 100%)를
  Chroma HNSW(14ms·recall 80%)와 나란히 둔다. 기본 파라미터로는 recall이
  68%였고, 색인 파라미터를 올려 80%를 시간과 용량으로 산 과정도 함께 남겼다
- **8장 되돌아가기**: 3주차 식단 플래너(`week03-meal-planner`)에 이번 주
  기법을 적용한 v2.0 → v2.1. 위키백과와 LLM으로 설명문을 채우고, 제약
  통과분을 유사도와 무관하게 되살리고, 계획자에게서 산수를 걷어내 73.4초를
  17.6초로 줄였다. 그리고 **검색을 꺼 보고 결과가 같은지 확인한 결산**까지
- **회전별 릴리즈와 데이터 산출물 배포**: v0.1 → v0.2(정제·파싱) →
  v0.3(PostgreSQL 검색) → v1.0(RAG) 순이다. 태그 하나가 그 지점의 코드와 데이터를
  함께 가리키고, 무거운 배치는 미리 돌려 둔 산출물을 받아 `compose up`으로
  재현한다

### 변경

- **4주차 주제 확장**: "데이터 수집·정제"에서 "데이터 수집·정제와 RAG
  검색"으로. 정제는 원칙만 얻고 빠르게 지나가며 무게중심을 RAG 검색으로 옮겼다
- **저장을 compose 서비스로**: 정형 저장을 SQLite에서 **PostgreSQL** `db`
  서비스로, 벡터 저장을 sqlite-vec 임베디드에서 **Chroma** `chroma` 서비스로
  바꿨다. 인프라를 숨기지 않고 서비스 추가 자체를 가르칠 거리로 삼는다
- **전량 실측치로 교체**: 개략치를 306,307행 × 166열 전수 조사 결과로 바꿨다.
  인코딩 오염 54건, 결측이 갈리는 38열과 125열, 섭취참고량 고유값 34개.
  추정으로 적었던 리포트 출력도 실제 실행 결과로 교체했다
- **재현자의 동선과 저자의 작업 순서 분리**: 회전마다 "수강생 재현"(배포
  산출물로 태그에 서기)과 "실행"(그 산출물이 어떻게 만들어졌나)을 나눠 적는다
- **session-04를 디렉터리 구조로**: `session-04.mdx`를 `session-04/index.mdx`로
  옮겼다
- **`docker-compose.dev.yml`을 진짜 override로**: 기본 파일 위에 겹쳐 쓰도록
  고쳤다

### 수정

- **키워드 검색의 페이지네이션 중복**: 정렬 키가 하나라 동점 구간의 순서가
  매번 달라져 1페이지의 행이 2페이지에 다시 나왔다. 유일한 값을 마지막 정렬
  키로 붙여 전순서로 만들었다
- **한글 질의의 URL 인코딩**: `curl "…?q=떡볶이"`는 uvicorn이 400으로
  거절한다. 예제를 `-G --data-urlencode`로 고쳤다. heredoc 관찰 명령에
  `docker compose exec -T`가 빠져 TTY 오류로 죽던 것도 함께 고쳤다
- **진행 막대 4/5**: 배포본을 받아 쓰면 원본 칸이 영원히 비어, 뒷단계가 다
  찼는데도 4/5로 남았다. 캡션에 `(배포본)`을 붙여 단조롭게 고쳤다
- **`nunique()` 서술 격하**: 근거를 재구성할 수 없는 배율을 걷어내고 파싱
  리포트 실측에 맞췄다. 절감은 접기와 규칙이 나눠 가진다는 단서를 함께 적었다
- **태그 체크아웃 방식 통일**: 재현 절과 실행 절이 서로 다른 명령을 쓰던 것을
  `git checkout <태그>`로 맞췄다. 브랜치는 직접 만들어 보는 선택 경로에만 남겼다

## [1.3.0] - 2026-08-06

### 추가

- **3주차 교안 (session-03)**: "LLM API와 프롬프트". 식약처 식품영양성분DB
  실데이터로 나트륨 예산 안에서 저녁 7끼를 짜는 "한 주 밥상" 식단 플래너 API를
  짓는다. 일부러 에이전트가 아닌 것, 곧 코드가 제어하는 파이프라인을 만들며
  나쁜 프롬프트의 실패를 컨텍스트 주입·스키마·few-shot으로 단계별 교정하고,
  계획자와 검증자를 다른 회사 모델로 교차시키며, instructor structured output과
  폴백 체인으로 안전 검사의 가용성을 지킨다
- **3주차 슬라이드 덱 (session-03)**: 교안 전 범위를 담은 102장짜리 라이브
  세션용 덱
- **4주차 교안 (session-04)**: "데이터 수집·정제". 3주차의 깨끗한 `foods.json`이
  어떻게 만들어졌는지 치우기 전의 원본에서 되짚는다. 식약처 가공식품 DB
  187MB·약 31만 건을 인코딩 정리·결측 정책·컬럼 슬림으로 정제하고, 자유 텍스트
  섭취참고량을 규칙 우선·LLM 잔여로 파싱하며, 정제 데이터셋을 SQLite에 굳혀
  검색 서비스로 소비한다. 정제는 코드가 아니라 리포트로 검증한다
- **session-03 v1.5 추가 과정**: 라이브 세션 범위 밖의 관측(observability) 확장을
  문서화했다. `POST /plan/stream`(SSE) 진행 이벤트, 코드 재계산 근거 `audit`,
  호출 기록 `llm_calls`, 역할별 모델 구성 `GET /config`
- **용어집 SSE 항목**: Server-Sent Events. 진행 이벤트 스트리밍 설명에서
  `[[sse]]`로 링크된다

### 변경

- **stack-site-builder 1.23.2로 업데이트**
- **교안 문체 전환**: session-01\~04를 발표 대본에서 독자용 자료로 다시 썼다.
  세션에 오지 못했거나 각자의 속도로 재현하는 사람이 문서만으로 따라올 수 있다
- **코드 다운로드를 GitHub Tags 페이지로**: 시연 저장소의 각 지점(v0.1·v1.0·v1.5)을
  태그 zip으로 내려받게 바꿨다. 재현자는 필요한 지점에서 시작한다
- **session-03을 디렉터리 구조로**: `session-03.mdx`를 `session-03/index.mdx`로
  옮기고 다운로드·샘플 데이터 스크린샷 3장을 곁에 뒀다
- **session-03 6장 도입부 교체**: 학생용 git flow 셋업을 걷어내고 v0.1과 v1.0의
  diff로 연다
- **데이터 준비를 부록으로**: 3주차 브리핑에서 데이터 준비 절차를 빼 부록으로
  옮겨, 세션 흐름이 본 주제에서 끊기지 않게 했다

## [1.2.0] - 2026-07-30

### 추가

- **2주차 교안 (session-02)**: "컨테이너 개발환경과 팀 워크플로". docker compose로
  app+db를 함께 띄우고, Git Flow 회전(feature 브랜치 → PR → CI → 머지)으로 이미
  동작하는 회의실 예약 서비스에 AI 에이전트를 얹는다. 기존 서비스 함수를 감싼
  도구(검색·가용·예약 생성·취소·예약 내역 조회), 자연어 채팅 UI, 대화 이어가기(DB
  무상태), 권한 코드 강제·재시도·폴백·루프 가드까지 다룬다
- **2주차 슬라이드 덱 (session-02)**: 커버·브리핑, 환경 투어, Git Flow 회전, 채팅
  UI, 예약 내역 조회, v1.5 SQL 콘솔, 마무리로 구성한 라이브 세션용 덱
- **LLM 멀티 프로바이더 구성**: session-02의 LiteLLM 층을 Gemini(기본
  `gemini/gemini-2.5-flash`)·Claude(`claude-3-haiku-20240307`)·OpenAI(`gpt-4.1-mini`)
  셋으로 확장하고, 프로바이더 키를 `.env`로 관리하며 실패 시 폴백 체인을 구성한다
- **v1.5 추가 과정 (관리자 전용, 선택)**: 자연어를 SQL로 옮겨 DB를 조회하는
  개발자·분석가용 도구. 전용 관리자 계정, 읽기 전용 롤, 단일 `SELECT` 파싱, 민감
  컬럼을 가린 리포팅 뷰의 다층 방어로 가둔다
- **session-02 "0. 사전 준비" 섹션**: git·Docker 설치 안내(Windows·Mac)

### 변경

- **stack-site-builder 1.23.1로 업데이트**
- **한글 줄표(`—`) 금지 규칙 도입**: `docs/writing-rules.md`에 규칙을 추가하고 전
  콘텐츠에서 줄표를 제거했으며, `scripts/check-style.py`가 이를 강제한다
- **리스트 선호·긴 문단 하드 브레이크 규칙 추가**: 나열형 내용은 리스트로 나누고,
  이어지는 긴 산문 문단은 하드 브레이크로 문장을 나눈다
- **1주차 자료(session-01) 정리**: 줄표 제거와 표현 다듬기 반영
- **VOD·영상 참조 정리**: 1·2주차 강의 자료에서 영상 아카이브 언급을 걷어내고
  코드·프롬프트 공개 중심으로 바꿨다

## [1.1.0] - 2026-07-25

### 추가

- **사용 도구 카탈로그**: 테마의 `stacks` 컬렉션으로 "사용 도구" 카탈로그를
  구성했다. 개발 도구(VS Code, Docker, Orca)와 참고 사이트(endoflife.date,
  awesome-ai-stack)를 카드·상세 페이지로 제공하고, 카테고리 브라우즈
  (`/categories/tools/`)로 묶어 본다. 헤더 내비의 "사용 도구" 항목과 홈 카드
  양쪽에서 진입한다
- **소개 페이지 "주요 링크 및 안내"**: Awesome AI Stack 링크와 LLM 프로바이더
  콘솔(Google AI Studio, Claude, OpenAI, Ollama, Groq) 참고 표

### 변경

- **stack-site-builder 1.22.0으로 업데이트**: cards 홈에서도 카탈로그 Browse
  내비를 쓰도록 `home.browse`를 지원한다. 이 값으로 헤더에 "사용 도구" 항목을
  붙여 native 카탈로그 페이지로 연결한다
- **명사형 종결 마침표 규칙 전면 적용**: 서술형(`합니다`/`입니다`)에만 마침표를
  붙이고 명사형(`트랙`/`정리` 등 체언 종결)에는 붙이지 않는다. 규칙을
  `docs/writing-rules.md`에 추가하고 문서 전반에 반영했다
- **홈 히어로 줄바꿈 정리**: 최대 폭에서 마지막 단어가 홀로 넘어가지 않도록
  세 줄 균형으로 조정했다

## [1.0.0] - 2026-07-25

### 추가

- **강의 사이트**: stack-site-builder 기반 Astro 사이트(`site/`)이며 한국어 단일
  로케일, cards 홈이며 강의·글·슬라이드·개념·용어집·소개 섹션을 쓴다
- **Docker 실행**: 저장소 루트에서 `docker compose up`으로 사이트를 구동하고,
  `docker compose -f docker-compose.dev.yml up`으로 편집·핫리로드로 본다
- **1주차 교안**: 상세 강의 문서(`session-01`)와 슬라이드 덱: AI 서비스
  전주기(5레이어), 엔지니어링 사다리(프롬프트 → 컨텍스트 → 하네스), 분식집 주문
  에이전트 라이브 빌드, v0.1/v0.2/v1.0 코드 구조 분석과 인젝션 방어, 프로젝트
  트랙, 개인 repo 실습. 시연 저장소 `week01-order-agent`와 연계한다
- **주차별 운영안**: 소개 페이지에 1\~9주(공통 역량)·10\~14주(개인 프로젝트) 표
- **글**: 안내글(로컬에서 강의 자료 보기)과 강의 노트(Docker/Docker Compose 입문)
- **용어집**: LLM·RAG·임베딩·벡터DB·에이전트·가드레일·프롬프트 인젝션·Docker 등
  본문에서 `[[용어]]`로 링크되는 용어 정의
- **문서·규칙**: 작성 규칙(`writing-rules`), 문서 규칙(`documentation-rules`),
  git flow(`git-workflow`), 문서 스타일 검사 스크립트(`scripts/check-style.py`),
  AI 도구용 가이드(`CLAUDE.md`)
